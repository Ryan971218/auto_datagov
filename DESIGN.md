# MeanFlow · 产品设计与 AI 角色文档

> 这是"含义流转平台"的产品设计指南。任何后续设计决策、任何 AI agent 接入这个项目，**先读这份文档**。

---

## 1. 一句话定位

**MeanFlow 不是"数据治理系统"，是"业务含义流转的基础设施"。**

数据治理的隐喻是"警察"——居高临下管控数据。
含义流转的隐喻是"水"——含义从业务方流到数据，从数据流到消费方，**我们让这条水管可见、可修复、可审计**。

---

## 2. 核心哲学（5 条不能动摇的原则）

### 原则 1：数据-含义 gap 是宇宙级的，不可消除，只能管理

```
bits (确定) ←── gap ──→ meaning (协商)
```

- bits 有 1/0 唯一答案
- meaning 是人协商出来的（"GMV 含不含未支付订单"是协商结果）
- 这条 gap **不是 bug，是 feature**——它是业务灵活性的来源

**设计推论**：系统不试图"权威化"含义定义，**只追踪含义的流转**。

### 原则 2：AI 是翻译器/检索器/校对器，不是"理解者"

```
人类  ──→  AI  ──→  系统
意图    翻译     持久化
```

- AI 不发明含义——**业务方发明**
- AI 不做判断——**人类做判断**
- AI 做：模式匹配、跨实体检索、SQL 生成、相似案例发现

**设计推论**：所有 AI 输出必须带溯源链接 + 置信度 + 人类反馈入口。

### 原则 3：ontology 是字典，不是宪法

- ontology **分类所有实体**（概念/资产/字段/方法论）——✓ 字典
- ontology **不规定业务流程**——✗ 宪法

**设计推论**：业务方定义 GMV 含义，系统**追踪**这个定义。如果业务方改含义，系统跟着改，但**不评判**。

### 原则 4：累积型机构记忆 + 自修剪

- 不承诺"涌现智能"
- 承诺"团队每天少踩一次坑"
- **自修剪 = 自生长**：3 个月没用的 axiom 自动降级

**设计推论**：所有"记忆"必须**有时间戳和废止机制**。

### 原则 5：AI 提议，人类 ratify

- 系统**不自动批准**任何变更
- LLM 提议 axiom → 人类 ratify → 才能 active
- 即使 AI 信心 99%，最终决定在人类

**设计推论**：所有 AI 输出必须有 👍/👎/📌 反馈按钮，反馈进入 log.md 作为下一轮训练信号。

---

## 3. 4 层架构（含义流转的物理形态）

```
┌─────────────────────────────────────────────────────┐
│ 接入层 (Source)        MySQL / PG / Hive / Kafka     │
├─────────────────────────────────────────────────────┤
│ 加工层 (Processing)     dbt / Airflow / Spark        │
├─────────────────────────────────────────────────────┤
│ 服务层 (Serving)        mart / ADS                  │
├─────────────────────────────────────────────────────┤
│ 应用层 (Application)    BI / API / ML                │
└─────────────────────────────────────────────────────┘

每层有 3 件事要做：
1. 知道我是谁（资产 = 我有这张表）
2. 知道我从哪来（血缘 = 上游是谁）
3. 知道自己对不对（质量 = axiom 通过了吗）
```

---

## 4. 含义流转的 5 步闭环

```
业务方  ──→  ① 概念定义   (concepts/gmv.md)
              ↓
         ② 物理承载  (assets/dwd.dwd_orders)
              ↓ 字段映射
         ③ axiom 校验 (A01/A14)
              ↓
         ④ 异常发现  (problems/2026-08-17)
              ↓
         ⑤ 自演化    (axiom 健康度 → 提议新 axiom)
              ↓
         ⑥ 修复沉淀  (SOP / template / axiom 升级)
              ↓
         回到 ①   (下次业务方变更时)
```

**关键**：第 ⑥ 步是核心——问题不能只是"被记录"，必须"被治理"。

---

## 5. AI 的角色分工（4 种任务 × 4 种模型）

| 任务类型 | 主路由 | 降级 | 触发降级条件 | 理由 |
|---|---|---|---|---|
| 简单 QA / 概念查询 | qwen-local | claude-sonnet | 本地 QPS > 50 | 本地 $0，数据不出域 |
| SQL 生成 | deepseek-coder | claude-sonnet | 代码 test 失败 > 10% | 代码专长，便宜 |
| 复杂推理（多跳） | claude-sonnet | gpt-4o | sonnet 不可用 | 200k context + 强推理 |
| 长上下文（>32k） | claude-sonnet 200k | 分块 + 多次 | 输入 > 200k | 单次跑完 |
| PII 敏感查询 | qwen-local | 拒绝服务 | 检测到 PII | 合规优先 |
| 所有远程失败 | llama-local | 拒绝 | 兜底链全失败 | 可用性 |

**核心原则**：
- 简单任务用本地（$0）
- 复杂任务用远程（贵但强）
- PII 任务永不外发
- 兜底链保证**永远有响应**

---

## 6. 自演化机制

```
每日 02:00
   ↓
LLM 扫描 log.md + problems/ + axiom 健康度
   ↓
提议 axiom 候选（CAND-007、CAND-008）
   ↓
写到 wiki/axioms/_candidates/
   ↓
人类 daily review（< 10 分钟）
   ↓
批准 → provisional → 30 天通过 → active
   ↓
拒绝 → 删除
   ↓
axiom 误报率 > 阈值 → 自动降级
```

**不要做**：
- ❌ LLM 自动批准任何变更
- ❌ 自动删除任何 axiom
- ❌ 自动回滚 schema

---

## 7. 4 种方法论形态

| 形态 | 用途 | 例 |
|---|---|---|
| **axiom** | 硬规则，可 lint | A01: 无直接 PII 字段 |
| **SOP** | 标准操作流程 | sop-gmv-001: 月度对账 |
| **template** | 可复用片段 | tpl-pii-mask: 脱敏 SQL |
| **flow** | 跨人协作流 | flow-001: 上游变更通知 |

**为什么 4 种而不是 1 种**：axio是机器可读的硬规则，SOP 是人的操作清单，template 是代码片段，flow 是组织流程。**真实的治理知识是混合的**。

---

## 8. 关键设计原则（做与不做）

### ✅ 做
- 每个 AI 输出带溯源 + 置信度
- 所有变更可审计（git commit + 批准链）
- 简单任务用本地模型（省钱 + 数据安全）
- 让业务方参与定义含义
- 接受"涌现智能不会发生"——承诺累积型记忆
- 区分"系统"和"集成层"——不重复造 dbt docs 的轮子

### ❌ 不做
- 不要让 AI 自动批准任何变更
- 不要做"权威字段字典"（让 dbt docs 做）
- 不要承诺"AI 比人更懂数据"
- 不要把治理系统做成"大而全 wiki"——**只做含义流转**
- 不要忽视业务方——没有他们，系统是死数据

---

## 9. 页面架构（产品地图）

| 层级 | 页面 | 解决什么 |
|---|---|---|
| 连接 | 数据源 / AI 模型配置 | 接入外部系统 |
| 流转全景 | 概览 / Wiki / 流转冲突 / AI 助手 | 全局视图 + 入口 |
| 核心实体 | 业务含义 / 数据资产 / 数据字典 | 含义 + 物理 + 字段 |
| 治理沉淀 | 问题库 / 方法论 / 治理深度 | 沉淀 + 血缘 + 质量 |
| 自演化 | 4 个场景 / 本体健康度 | 系统自反馈 |
| 操作 | 任务调度 / 监控告警 / SQL 查询 / 脚本管理 | 数据工程师日常 |

---

## 10. 对 AI agent 的具体指令

如果你是 AI agent 接入这个项目：

### ❌ 不要做
- 不要把"概念"和"资产"混为一谈
- 不要把"系统能查"当作"系统能做决定"
- 不要假设所有业务方都愿意配合
- 不要把 axiom 当成"可以被忽视的建议"
- 不要让任何变更绕过批准链

### ✅ 要做
- **先读这份文档**，再读 AGENTS.md
- 任何 AI 输出 → 必须有溯源（哪个 wiki 页 / axiom / 问题）
- 任何对业务含义的修改 → 必须标记 `business_owner`，不能擅自
- 任何"自演化" → 必须经过人类 ratify
- 找不到答案时 → 明确说"不知道"，不要编

### 决策树
```
接到新需求
  ↓
问：这是含义问题还是物理问题？
  ├─ 含义 → 走 concepts/ → 需要业务方
  └─ 物理 → 走 assets/ → 可以查 schema
  ↓
问：这是规则问题还是流程问题？
  ├─ 规则 → axiom (机器可读) + lint
  └─ 流程 → SOP / template / flow (人参与)
  ↓
问：这是已知问题还是新问题？
  ├─ 已知 → 查 problems/ + axioms/ 找 SOP
  └─ 新 → 记录到 problems/，不直接给方案
  ↓
任何时候遇到边界 → 问"是治理边界还是技术边界？"
  ├─ 治理 → 不能替人类决定
  └─ 技术 → 可以建议
```

---

## 11. 三个关键判断（产品设计的天条）

### 判断 1：**系统是整合层，不是权威源**
- 字段定义权威源是 dbt docs / Hive metastore
- 含义权威源是业务方 wiki
- 系统**整合这些源**，加上治理注解
- 试图做"权威" = 失败

### 判断 2：**深度 > 广度**
- 100 个 axiom 不如 10 个**真用**的 axiom
- 字段级血缘只做 GMV 链路一条，比做全表全字段有用
- 优先**少而精**，不要多而散

### 判断 3：**真实 vs 完美**
- prototype 阶段不要追求后端真实化
- 真实 vs 完美的权衡：**先真实（mock 但能跑），再完美（加后端）**
- 字段级血缘是"用表格还是图"——**两个都给**，比只给一个完美

---

## 12. 验收标准（这个产品算"做好"了）

✅ **含义流转闭环跑通**：业务方改 GMV → 系统自动通知所有下游 → 下游确认后追溯到 axiom → axiom ratify → 写入 axiom 历史

✅ **AI 是协作者不是决策者**：所有 AI 输出可被人类 override，所有重大决策有批准人

✅ **数据-含义 gap 可见**：每个字段都能溯源到业务概念，每个业务概念都能追溯到字段

✅ **自演化是 conservative 的**：从不自动批准，自动降级误报高的 axiom，30 天不用的提议删除

✅ **审计链完整**：每个变更都有 author + approver + git commit + 影响面

---

## 13. 这个文档本身

- 这不是"技术规范"——是"设计哲学"
- 这不是"开发指南"——是"为什么要这样设计"
- 这不是"用户手册"——是"AI agent 接入项目前必读"

如果新增功能，**先问**：
- 这符合 5 条原则吗？
- AI 在这里扮演什么角色？
- 人类在哪一环介入？

如果答案让项目偏离这 5 条原则——**不实现**。

---

*最后更新：2026-08-19 · 基于 MeanFlow prototype 阶段讨论*
