# 对话压缩纪要

> 一次长聊的关键决议：从混乱到落地的全过程。读这一份就够了。

---

## 一、缘起（你最初的痛点）

你是数据工程师，工作中做 **接入 / 开发 / 运维 / 零碎需求**。两个真问题：
1. **责任边界不清** → 扯皮多
2. **规范性不高** → 出问题概率大

你想要的不是工具升级，而是 **数据工程本身能提供更高价值**：
- 入库阶段就能给指导
- 开发阶段能提建议
- 运维阶段能快速定位
- 数据资产能快速沉淀

最终立意：**自生长 = 问题沉淀方法论，下次类似问题能快速定位和处理**。

## 二、思维模型的三把刀

| 工具 | 含义 | 用在哪 |
|---|---|---|
| **本体论 Ontology** | 实体字典（库/表/字段/脚本/流程/角色/指标的字典） | 描述数据长啥样 |
| **自生长 Self-growing** | AI 与人共写一份"治理成果 Wiki" | 沉淀知识 |
| **第一性原理** | 唯一真神，判断架构是否成立的尺子 | 验证架构 |

> 第一性原理验证了三件套是分工合理的；
> ontology 和自生长不是互相指导，是 **被第一性原理统一** 在分工里。

## 三、关键认知纠偏（我们达成的共识）

| 误区 | 共识 |
|---|---|
| "涌现智能" 当立意 | ❌ 没用，是糊弄人的目标 |
| 让 AI 自主决策 | ❌ AI 是翻译/检索，不是决策者 |
| ontology 是宪法、定义流程 | ❌ 它是字典，记录含义 |
| 自生长 = 自动审批 | ❌ AI 提案，**人批准**，绝不自动 |
| 业务指标（GMV 等）属于本系统 | ❌ 它属于业务侧，本系统只是消费方 |
| 字段级血缘做成图 | ❌ 用**表**呈现，便于扫读；要做"追"的视图再单独画图 |
| 数据模型管理 | ❌ 太像 dbt docs，价值低，不做 |

## 四、产品定位（一句话）

> **含义流转平台**：把数据世界里"含义的流转"管理起来。
> AI 是认知助手（翻译、检索、起草），人才能拍板。

## 五、四层架构

```
接入层 (Source)  →  MySQL / Kafka / Hive / Flink / Spark / 业务库
加工层 (Process) →  ETL / dbt / SQL / Python / Shell / Notebook
服务层 (Serving) →  API / 报表 / 指标 / 模型
应用层 (App)     →  看板 / 问答 / 自助分析
```

每层都对应：**实体字典（ontology） + 含义流转（自生长） + 数据问题（入口）**。

## 五、5 步含义流转闭环

```
1. 定义概念（业务说"什么是有效订单"）
2. 映射物理（落到表 / 字段 / 脚本）
3. 公理校验（axiom：唯一、非空、范围…）
4. 冲突检测（口径不一致 / 链路断了）
5. 自生长（沉淀 SOP / 模板 / 流程，下次复用）
```

> 入口是**问题**，不是**资产**。这两条路我们选择了问题优先。

## 六、四种方法论载体

| 形式 | 干什么 | 例子 |
|---|---|---|
| **axiom 公理** | 静态校验规则 | 订单状态必须在 {1,2,3} |
| **SOP 标准作业** | 多步操作程序 | "新接入业务库" 的 7 步 |
| **template 模板** | 代码 / SQL 脚手架 | dbt model 模板 |
| **flow 流程** | 跨角色作业图 | 需求→开发→上线→运维 |

## 七、AI 在系统中的角色（很重要）

AI **始终**是：

| 可以做 | 绝不做 |
|---|---|
| 翻译业务语言 → 技术语言 | 直接拍板定口径 |
| 检索已有 SOP / 字典 / 血缘 | 自主写规则进 axiom |
| 起草 SOP / 模板草案 | 自动审批任何变更 |
| 解释一个字段为什么这么算 | 把责任推给数据工程师 |
| 提示"这个口径和你昨天的不一致" | 接管业务指标的所有权 |

> 强制要求：AI 的每一句输出都带 **出处（provenance） + 置信度**，人给它反馈形成自生长。

## 八、AI 模型路由（原型配置）

| 模型 | 用途 | 触发条件 |
|---|---|---|
| **qwen-local** | 简单查询 / PII 数据 | 命中敏感数据 / 短问答 |
| **deepseek-coder** | SQL / dbt 生成 | 涉及代码生成 |
| **claude-sonnet** | 长上下文推理 / 跨表解释 | 字段级血缘解释 |
| **gpt-4o** | 兜底 | 都没命中 |
| **llama-local** | 离线兜底 | 网络断 |

> **不允许 AI 自主调用，只有 dispatcher 路由。** 数据 owner 配置可见范围。

## 九、原型里已有的东西（按模块）

- ✅ 18 个页面（概览 / 实体字典 / 数据资产 / 脚本 / 含义 / 问题 / 治理深度 / 等等）
- ✅ 数据源接入（MySQL / Kafka / Hive / Flink / Spark / PG / MySQL）
- ✅ AI 模型接入（5 个模型的路由配置）
- ✅ 数据字典 + 资产（库/表/字段/BI/dbt/API）
- ✅ 脚本中心（Python / dbt / SQL / Shell / Notebook）
- ✅ 治理深度（血缘 + 质量校验，两根支柱）
- ✅ 审计 / 版本变更
- ✅ AI 问答面板入口
- ✅ 知识 Wiki 入口

## 十、还没做但未来一定要做

1. 真实的模型路由后端（现在是 mock）
2. 真实数据源连接器（现在是 demo data）
3. 字段级血缘的"追"视图（独立 SVG 图）
4. AI 行动历史 / 反馈打分入库
5. 多租户与权限
6. 移动端 / 嵌入式

---

## 十一、最终一句话

> 你要的不是"数据治理工具"，是**"含义工厂"**——让每个数据工程动作都自动留下"为什么这么做"的痕迹，让下次类似问题不必重新踩坑。
> AI 是车间工人，你是车间主任兼厂长，所有权在人。
